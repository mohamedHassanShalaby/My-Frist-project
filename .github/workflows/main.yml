on:
  push:
    branches: ['master']




jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: 'login to Docker hub'
        run: docker login --username DOCKER_USERNAME --password dckr_pat_dXFjFvyOffM1mVan8kEaTfdyug0